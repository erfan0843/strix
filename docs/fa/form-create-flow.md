# ساخت فرم — جریان کارشناس و مدیر

> مرجع «قدم اول». هر وقت در ادامهٔ کار به این مرحله برگشتیم، همین سند مبنا است و به‌روزش می‌کنیم.
> نمونهٔ اجرایی: `docs/fa/nora-ui/create.html` (سازنده) · `docs/fa/nora-ui/form.html` (نمای کاربر)

---

## ۱) نقطهٔ شروع — از دلِ کار

مدیر وسط کارش است: رویداد تعریف می‌کند، دوره می‌سازد، گزارش می‌خواهد. همان‌جا دکمه‌ای می‌بیند با عنوان همان کار («ساخت فرم ثبت‌نام»، «ساخت فرم مسابقه»…). با زدنش وارد فرم‌ساز می‌شود و **زمینه از قبل پر است**: نام، تاریخ، نشانی، ظرفیت.

فرمی که از رویداد می‌آید **همیشه به صفحهٔ رویداد پیوند دارد**؛ محدودیت‌هایش هم زنده سینک می‌شود.

## ۲) چهار مرحله، نه بیشتر

| مرحله | نام | داخلش |
| --- | --- | --- |
| ۱ | مدل و نام | منوی کاشی نُه مدلی · عنوان (پیش‌فرض از رویداد، قابل تغییر) · نشانی (پیش‌فرض تو در تو با رویداد، دلخواه سازنده) · پیوند و سینک |
| ۲ | فیلدها و تنظیمات | بسته‌های آماده · بوم چیدمان · **تنظیمات بیشتر، همین‌جا کنار فیلدها** |
| ۳ | دوره و پرداخت | گزینه‌های قیمت‌دار با تخفیف · تعداد بلیت · کد تخفیف · روش‌های پرداخت · تأیید و محافظ |
| ۴ | مرور و انتشار | نگاه آخر · نشانی و کیوآرکد · فهرست کارها · انتشار |

قاعده: **مرحله بیشتر نمی‌سازیم و توضیح اضافه نمی‌نویسیم.**

## ۳) بوم چیدمان — ترتیب دقیق

۱) **سربرگ** ثابت، پیش‌فرضش نام فرم
۲) **توضیحات ابتدا** — فیلد ثابت و غیراختیاری، رسانه‌پذیر
۳) **بعلاوهٔ افزودن فیلد**، درست زیر توضیحات ابتدا
۴) **فیلدها** — هر فیلد یک بلوک با دستگیره و نوار فرمان (بالا/پایین/تنظیمات/کپی/حذف)
۵) **بسته‌های پیش‌فرض همین فرم** + همان بعلاوه = افزودن فیلد + «ذخیره به‌عنوان بستهٔ سازمانی»

**بسته‌ها:** پایه (۳ فیلد) · کامل (۷ فیلد، پیشنهاد ما) · کامل‌تر (۱۲ فیلد، کامل‌ترین) · خالی. هر بستهٔ کامل‌تر برجسته‌تر دیده می‌شود و مدیر هم می‌تواند بستهٔ سازمانی خودش را بسازد.

## ۴) فیلد — ویژگی ساختاری و تنظیمات

هر فیلد بنا بر نوعش **ویژگی ساختاری** خودش را دارد؛ سازنده لازم نیست چیزی بسازد، فقط تأیید می‌کند:

| فیلد | ساختار |
| --- | --- |
| موبایل | باکس تلفن، ماسک ۰۹، صفحه‌کلید تلفنی — **تأیید کد پیامکی اختیاری، پیش‌فرض خاموش** |
| کد ملی | ده رقم با رقم کنترل |
| عدد / مبلغ | صفحه‌کلید عددی، جداکنندهٔ هزار، حد کم و زیاد |
| تاریخ / ساعت | تقویم شمسی، ساعت ۲۴ ساعتی |
| تک‌انتخاب / چندانتخاب / کشویی | گزینه‌ها |
| تصویر / فایل | آپلود با سقف تعداد |
| امتیاز | پنج ستاره |
| **انتخاب قیمت‌دار** | گزینه + مبلغ + تخفیف |
| مقررات | پذیرش اجباری |

** رنگ و شکل فیلد اجباری:** نوار رنگی در لبهٔ آغاز بلوک + برچسب «اجباری» + نقطهٔ رنگی کنار عنوان (در فرم کاربر هم همان نقطه تکرار می‌شود).

سایر تنظیمات: عنوان · راهنما (با **پیش‌فرض آماده** برای هر نوع) · رسانه (تصویر/فایل/ویدیو) · پر شدن از پروفایل · ستون خروجی اکسل · **شرط نمایش** («اگر [فیلد] [عملگر] [مقدار] پرسیده شود»).

## ۵) تنظیمات بیشتر — کنار همان فیلدها

در مرحلهٔ ۲، کنار بوم، گروه‌های بازشو (همه بسته به‌صورت پیش‌فرض، بی‌شلوغی):

نمایش و جهت · بازه و ظرفیت (شامل **تعداد لیست انتظار**) · محدودیت افراد · بلیت (فعال‌سازی، مدل، رنگ + پیش‌نمایش) · ارسال به کارشناس · متن پایانی · رفتار بعد از ثبت.

**محدودیت افراد** چهار حالت دارد: آزاد · فقط ثبت‌نام‌کرده‌های رویداد · فقط حاضران جلسهٔ پیشین · فهرست افراد یا کد دعوت — و **سینک زندهٔ آن با رویداد** روشن است.

**ارسال به کارشناس:** پیش‌فرض از بخشی که فرم از آن آمده (مثلاً بخش آموزش) و قابل تغییر دستی.

## ۶) مرحلهٔ دوره و پرداخت

**گزینه‌های قیمت‌دار** فیلد ثابتی است که سازنده اضافه می‌کند (نه گزینهٔ ثابت سیستم). هر گزینه: عنوان، مبلغ، **درصد تخفیف**. کاربر می‌تواند یکی، دو یا چند گزینه را با هم بردارد.

نمایش قیمت به سبک فروشگاهی: **مبلغ اصلی خط‌خورده**، **برچسب درصد تخفیف**، و **مبلغ نهایی بزرگ‌تر** کنارش.

**تعداد بلیت:** سازنده تعیین می‌کند «هر کس چند بلیت می‌تواند بگیرد» (مثلاً ۳). در فرم، ته صفحه یک دکمهٔ بزرگ **«افزودن همراه»** می‌آید؛ کاربر اطلاعات هر نفر را وارد می‌کند، **مبلغ در تعداد ضرب می‌شود** و کنار عدد نوشته می‌شود **«برای ۳ نفر»**.

**کد تخفیف:** کد و درصد، با انتخاب اینکه روی همهٔ نفرات حساب شود یا فقط نفر اصلی.

**روش‌های پرداخت:** درگاه‌ها را مدیر سامانه تعریف می‌کند؛ سازنده فقط تیک می‌زند: کارت‌به‌کارت · درگاه رسمی بله · زرین‌پال · پی‌پینگ · اقساط.

**محافظ‌ها:** کف و سقف مبلغ · تأیید دستی رسید کارت‌به‌کارت · قاعدهٔ «تأیید نشد ⇒ بلیت باطل، ثبت‌نام از ابتدا».

## ۷) نمایش مبلغ — قرار قطعی

**عدد به ریال · تومان به حروف، ریزتر زیرش.**

مثال: `۱٬۹۵۰٬۷۵۰ ریال` و زیرش با قلم ریزتر: «صد و نود و پنج هزار و هفتاد و پنج تومان».

همین قاعده در سه جا: صورت‌حساب فرم، صفحهٔ کارت‌به‌کارت (کنار دکمهٔ کپی)، و صفحهٔ مرور سازنده.

## ۸) سفر کاربر تا بلیت

لندینگ → سوال‌ها → **نوع شرکت و همراه** → صورت‌حساب → روش پرداخت → (کارت‌به‌کارت: کارت گرافیکی + کپی شمارهٔ کارت + کپی مبلغ دقیق + تومان به حروف) → رسید + یادداشت برای کارشناس → «اطلاعات شما ثبت شد» با هشدار صریح **بلیت موقت** → تأیید کارشناس → بلیت‌ها (به تعداد نفرات، کیوآرکد، **دانلود همه**) + پیام کارشناس و پیوندها → در صورت رد: صفحهٔ باطل با دلیل و دکمهٔ شروع دوبارهٔ پرداخت.

**با درگاه رسمی بله:** پرداخت همان لحظه، بلیت **قطعی** است و فقط برای اطلاع در کارتابل کارشناس می‌نشیند.

## ۹) تصمیم‌های قطعی‌شده

| # | پرسش | تصمیم |
| --- | --- | --- |
| ۱ | بعلاوهٔ زیر توضیحات ابتدا | فهرست کامل فیلدها؛ بسته‌ها همان پایین بوم |
| ۲ | بسته‌ها | سه بستهٔ آماده + امکان ساخت بستهٔ سازمانی |
| ۳ | محدودیت افراد | هر سه حالت + کد دعوت، به انتخاب سازنده، سینک با رویداد |
| ۴ | کارشناس مسئول | پیش‌فرض بخشی، قابل تغییر دستی |
| ۵ | درگاه رسمی بله | بلیت قطعی؛ فقط اطلاع در کارتابل |
| ۶ | مبلغ همراه | مبلغ انتخاب‌های نفر اصلی × تعداد نفرات |
| ۷ | نمایش مبلغ | عدد به ریال، تومان به حروف ریزتر زیرش |
| ۸ | تخفیف | روی خود گزینه‌ها، به سبک فروشگاهی (خط‌خورده + درصد + نهایی) |
| ۹ | تنظیمات بیشتر | همین‌جا کنار فیلدها؛ کل جریان چهار مرحله |
| ۱۰ | نشانی فرم | پیش‌فرض تو در تو با رویداد، دلخواه سازنده |
| ۱۱ | تأیید موبایل | اختیاری؛ سازنده خودش روشن می‌کند |
| ۱۲ | لیست انتظار | تعدادش دست سازنده |

## ۱۰) واژه‌نامه

فیلد · عنوان · راهنما · بستهٔ پیشنهادی · شرط نمایش · کارشناس · سازندهٔ فرم · کاربر · مبلغ دقیق · تومان به حروف · بلیت موقت

## ۱۱) کارهای باقی‌مانده (ادامه از اینجا)

۱. ریزهزینه روی گزینه‌ها (ارسال پستی گواهینامه و مانندش)
۲. ظرفیت جدا برای هر گزینه (۲۰ نفر با گواهینامه، ۴۰ نفر بدون)
۳. مرحله‌بندی فرم (چند گروه سوال با نوار پیشرفت)
۴. تکرارشونده (اعضای خانواده، اقلام سفارش)
۵. پایپینگ («{نام} عزیز…»)
۶. ظرفیت مشترک دو فرم روی یک رویداد
۷. نسخهٔ فرم و اثرش بر پاسخ‌های قدیم
۸. کد تخفیف اختصاصی برای معرف‌ها و گروه‌ها
۹. فاکتور رسمی قابل چاپ
۱۰. متن پیام‌ها (تأیید، رد، یادآوری مهلت، یادآوری نیمه‌کاره)
۱۱. گواهینامهٔ کارگاه با شرط صدور
۱۲. آیدی‌کارت
۱۳. گزارش قیف با ریزش هر سوال
