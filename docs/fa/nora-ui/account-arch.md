# معماری بخش «حساب من» و صفحهٔ ورود

دو صفحه که یک کار را پیش می‌برند: `login.html` حساب را باز می‌کند و
`account.html` حساب را نشان می‌دهد. نورا پی یک ردیف است، نه کارت جدا؛
پشتیبانی هم صفحهٔ خودش (support.html) و نوار چسبیده به نوار پایین.

## ۱) صفحهٔ ورود (login.html)

یک کارت، سه پله، بی رمز:

```
login.html
├── پلهٔ ۱ شماره        برچسب «تلفن همراه خود را وارد کنید»، کادر با پیش‌شمارهٔ ۰۹
│                      و نشان گوشی، دکمهٔ «ورود»، جداکنندهٔ «ورود با»، سه پیام‌گیر
│                      (بله، ایتا، تلگرام)، و پیوندهای راهنما · پشتیبانی · قوانین
├── پلهٔ ۲ تحویل و کد    شماره به پیام‌گیر انتخابی تحویل می‌شود: نام پیام‌گیر،
│                      شمارهٔ پوشیده، نقطه‌های انتظار، دکمهٔ «باز کردن بله/ایتا»،
│                      کادر کد پنج‌رقمی، شمارندهٔ دو دقیقه، «دوباره بفرست»،
│                      «تغییر شماره»
├── پلهٔ ۳ پایان        نشان تیک، «خوش آمدی»، و «رفتن به حساب من»
└── حال‌های دیگر       کسی که همین حالا وارد شده: کارت «همین حالا وارد شده‌ای»
                       با خروج؛ ورود نیمه‌کاره: ادامه از پلهٔ کد
```

- دکمهٔ هر پیام‌گیر با شمارهٔ درست، همان را تحویل می‌دهد؛ با شمارهٔ ناقص، خطا
  می‌دهد و کادر شماره را می‌آورد. شماره بی صفر و ۰۹ هم پذیرفته می‌شود
  (`9123456789` ← `09123456789`).
- کد نمونهٔ پیش‌نمایش `۵۴۳۲۱` است، همان کدی که ورقهٔ ورود در `ui.js` می‌پذیرد.
- نشست در همان کلیدهای آشنای برنامه می‌نشیند: `nora-home-user` نوشته می‌شود و
  `nora-home-auth` (پلهٔ نیمه‌کاره) پاک می‌شود؛ پس بقیهٔ صفحه‌ها همان کاربر را
  می‌بینند.
- بازگشت: `login.html?next=account.html#club` — و نشانی بیرونی پذیرفته نمی‌شود.
- ورقهٔ «قوانین» از `ACCOUNT.login.rules` پنج بند کوتاه می‌سازد.
- نگاره‌های پیام‌گیرها SVG درون‌خطی‌اند: بله (سبز، تیک)، ایتا (تیره با قفل نارنجی)،
  تلگرام (آبی، کاغذپران).

## ۲) شکل کار حساب

```
account.html                  سرِ پروفایل: نوار باریک بالا با نشان تأیید و امتیاز،
│                             آواتار با نقطهٔ آنلاین، نام، شماره و تاریخ عضویت،
│                             نشان سطح، سه عدد، نوار تکمیل اطلاعات، دو کنش
├── میان‌بُرها                 سه کاشی: کارت ورود، دعوت دوست، پشتیبانی
├── رویداد نزدیک              کارت تاریخ‌دار رویداد بعدی + دکمهٔ کارت ورود
├── نورا پی           #pay    ردیف اول بخش‌ها — فاز بعد، بی‌ورود هم باز
├── رویدادهای من      #events پنج تب: پیش‌رو، برگزارشده، بلیت و گواهی، کارنامهٔ حضور، نظرها
├── پروفایل من        #profile شش تب:
│     ├── اطلاعات من        هویتی، تماس، نشانی + فرایند تأیید (پیش‌نویس → صف → تأیید)
│     ├── ورود و امنیت      شمارهٔ ورود، بی‌رمزی، راه‌های ورود با پیام‌گیر،
│     │                     دستگاه‌های واردشده (بستن نشست) و خروج از حساب
│     ├── امتیاز و سطح      امتیاز، نردبان سطح‌ها، دستاورد، فروشگاه پاداش، دعوت
│     ├── باشگاه کتاب‌خوانی کتاب ماه، پیشرفت مطالعه، صندلی، یادداشت، رأی، جلسه‌ها، قفسه
│     ├── فرم‌های من         پیش‌نویس، در صف، تأییدشده
│     └── حریم خصوصی       دانلود اطلاعات، بسته‌شدن حساب (کلمهٔ «حذف» + کد پیامکی)
└── پشتیبانی و راهنما #support نوار چسبیده به نوار پایین ← صفحهٔ پشتیبانی
```

نشانی‌های کوتاه با `ALIAS` به تب خودشان می‌روند: `#club` (امتیاز و سطح)،
`#book` (باشگاه کتاب‌خوانی)، `#auth` (ورود و امنیت)، `#forms`، `#privacy`،
`#info` و `#account` (اطلاعات من). نشانی ناشناس، پیام می‌دهد و به پروفایل
برمی‌گردد.

## ۳) مهمان و عضو

- مهمان: کارت «خوش آمدی» با سه فهرست کوتاه و یک دکمه: «ورود با شمارهٔ موبایل».
  زدنش به `login.html?next=account.html` می‌رود و بعد از ورود همین صفحه
  برمی‌گردد؛ اگر روی ردیفی زده باشد، همان بخش باز می‌شود (`S.after`).
- نورا پی و پشتیبانی برای مهمان هم باز است؛ بقیهٔ ردیف‌ها اول ورود می‌خواهند.
- خروج از حساب در تب «ورود و امنیت» است: پیش از انجام، تأیید می‌گیرد و فقط
  `nora-home-user` را پاک می‌کند؛ بلیت و گواهی و امتیاز دست‌نخورده می‌ماند.

## ۴) پشتیبانی

- صفحهٔ جدای خودش است: `support.html`. نوار پایین حساب و نشان نوار بالا به
  همان‌جا می‌روند و در `home.html` و `events.html` و `event.html` هم نشان
  پشتیبانی کنار اعلان‌ها نشسته.
- داخل حساب دیگر ورقه‌ای برای پشتیبانی نیست؛ فقط `goSupport()` و
  `supportVisible()` مانده که نوار و نشان را از داده خاموش می‌کند
  (`ACCOUNT.support.open=false` و نشانی‌اش در `ACCOUNT.support.href`).
- ورقهٔ کوچک ورود در `ui.js` (برای خرید و پیش‌ثبت‌نام) سرِ جای خودش هست، ولی
  پیوند «ورود با بله و ایتا و تلگرام» دارد که به `login.html` می‌رود.

## ۵) داده

همهٔ عدد و برچسب از `data.js`: `ACCOUNT.sections` (چهار ردیف)،
`ACCOUNT.profileTabs` (شش تب)، `ACCOUNT.login` (نام پیام‌گیرها، نشانی‌شان،
جملات پله‌ها و پنج بند قوانین)، `ACCOUNT.policy`، `ACCOUNT.fields` و `flow`،
`CLUB`، `ACH`، `levels`، `CERTS`، `FAQ`، `score`. صفحه عدد سخت‌کد نمی‌کند
مگر برای نمونهٔ دموی جلسه‌های آخر و فهرست دستگاه‌ها.

## ۶) دسترس‌پذیری

- تب‌ها `role="tab"` و `aria-selected` دارند و فقط تب روشن `tabindex="0"` است.
- کلیدهای جهت‌دار (راست و چپ، با احتساب راست‌به‌چپ)، `Home` و `End` بین تب‌ها
  می‌چرخند؛ `Esc` ورقه‌ها را می‌بندد.
- کادر شماره `inputmode="numeric"`، `autocomplete="tel-national"` و برچسب
  دارد؛ کادر کد `autocomplete="one-time-code"` و پیام‌ها در `#lgStatus` با
  `aria-live` خوانده می‌شوند.
- هر پیام‌گیر دکمهٔ برچسب‌دار است و پیوند بیرونی‌اش `rel="noopener"` دارد.

## ۷) آزمون

`node login.test.mjs` — ۶۶ بررسی: شکل کارت، پلهٔ شماره و خطاهایش، تحویل به
سه پیام‌گیر، پلهٔ کد، حال‌های آغازین، بازگشت به بخش خواسته‌شده، ورقهٔ قوانین و
پیمان با `account.js` و `sw.js`.
`node account.test.mjs` — ۱۲۵ بررسی: سرِ پروفایل، میان‌بُرها، تفکیک بخش‌ها،
شش تب پروفایل (با ورود و امنیت و خروج)، نشانی‌ها، مهمان، صفحهٔ پشتیبانی،
رویدادهای من، امتیاز و باشگاه کتاب، اطلاعات و حریم خصوصی، دسترس‌پذیری و
درستی داده و نماد.
`node support.test.mjs` (۱۷۰) و `node smoke.test.mjs` (۶۴۳) و
`node qr.test.mjs` (۱۰) هم کنار همین‌ها اجرا می‌شوند.
